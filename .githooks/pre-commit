#!/bin/sh
# SPDX-License-Identifier: MIT

set -eu

ROOT=$(git rev-parse --show-toplevel)
cd "$ROOT"

if [ ! -d node_modules ]; then
  echo "error: dependencies are not installed; run 'make bootstrap'" >&2
  exit 1
fi

make format-check lint typecheck check-spdx
